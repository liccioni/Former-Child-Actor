// The core actor runtime: ActorSystem, ActorRef, Mailbox, Dispatcher.
// This module must have no dependency on any other framework module.

description = "Minimal local actor runtime (ActorSystem, ActorRef, Mailbox, Dispatcher)."
